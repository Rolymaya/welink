# Backend Environment Variables
# IMPORTANT: Copy this file to .env and update with your actual values
# NEVER commit .env to version control!

# Application
NODE_ENV=development
PORT=3001

# Database - UPDATE WITH STRONG PASSWORD!
DATABASE_URL="mysql://root:YOUR_STRONG_PASSWORD_HERE@127.0.0.1:3306/saas_db"

# JWT Authentication - GENERATED SECRETS (DO NOT SHARE!)
# Secret 1 (generated):
JWT_SECRET="c972fe64405d307e84dd897c54ae9b65e0933d845e36e795481562bada58486215a7fc98869c77fbf3869e3ee5e68459870c08daeb03e2dd21b8101dd09abdb9"
JWT_EXPIRATION="1d"

# Secret 2 (generate new one with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))")
JWT_REFRESH_SECRET="GENERATE_YOUR_OWN_SECRET_HERE"
JWT_REFRESH_EXPIRATION="7d"

# URLs
FRONTEND_URL="http://localhost:3000"
BACKEND_URL="http://localhost:3001"

# SMTP Configuration (for email notifications)
# For Gmail: Enable 2FA and create App Password
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
SMTP_FROM="WeLinkAI <noreply@yourdomain.com>"
SMTP_SECURE="false"

# Redis (if using)
REDIS_HOST="localhost"
REDIS_PORT="6379"

# Weaviate Vector Database
WEAVIATE_URL="http://localhost:8080"
WEAVIATE_API_KEY="your-secure-api-key-here"

# File Upload
MAX_FILE_SIZE="52428800"  # 50MB in bytes
UPLOAD_DIR="./uploads"

# Production Notes:
# 1. Generate new JWT secrets for production
# 2. Set strong database password
# 3. Update FRONTEND_URL to your production domain
# 4. Configure SMTP with real credentials
# 5. Set Weaviate API key
